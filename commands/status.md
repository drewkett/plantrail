---
description: Show the plantrail thread bound to this directory
allowed-tools: Bash(node:*)
---
!`node "${CLAUDE_PLUGIN_ROOT}/server/dist/cli.js" status`

Summarize the thread state above in a few lines: the goal, what's in progress, and the last checkpoint. Don't take any action.
