import{createRequire}from"module";const require=createRequire(import.meta.url);
import{parseArgs as G}from"node:util";import{chmodSync as P,mkdirSync as Y,readFileSync as J,writeFileSync as q}from"node:fs";import{join as I}from"node:path";import{fileURLToPath as V}from"node:url";import{DatabaseSync as M}from"node:sqlite";import{mkdirSync as F}from"node:fs";import{homedir as H}from"node:os";import{join as A}from"node:path";function O(){return process.env.AUTOPLAN_HOME??A(H(),".autoplan")}var L=[`
  CREATE TABLE counters (name TEXT PRIMARY KEY, next INTEGER NOT NULL);
  INSERT INTO counters VALUES ('t', 1), ('n', 1);

  CREATE TABLE threads (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    goal TEXT NOT NULL DEFAULT '',
    status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active','parked','done')),
    created_at TEXT NOT NULL,
    touched_at TEXT NOT NULL
  );

  CREATE TABLE nodes (
    id TEXT PRIMARY KEY,
    thread_id TEXT NOT NULL REFERENCES threads(id),
    parent_id TEXT REFERENCES nodes(id),
    kind TEXT NOT NULL DEFAULT 'task' CHECK (kind IN ('task','question','finding','decision')),
    title TEXT NOT NULL,
    status TEXT NOT NULL DEFAULT 'open' CHECK (status IN ('open','active','blocked','done','abandoned')),
    summary TEXT,
    body TEXT,
    refs TEXT,
    priority INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
  );
  CREATE INDEX nodes_thread ON nodes(thread_id, status);
  CREATE INDEX nodes_parent ON nodes(parent_id);

  CREATE TABLE edges (
    from_id TEXT NOT NULL REFERENCES nodes(id),
    to_id TEXT NOT NULL REFERENCES nodes(id),
    type TEXT NOT NULL CHECK (type IN ('blocks','answers','derived_from','contradicts')),
    PRIMARY KEY (from_id, to_id, type)
  );
  CREATE INDEX edges_to ON edges(to_id, type);

  CREATE TABLE links (
    thread_id TEXT NOT NULL REFERENCES threads(id),
    kind TEXT NOT NULL CHECK (kind IN ('repo','dir','url','ticket')),
    value TEXT NOT NULL,
    PRIMARY KEY (thread_id, kind, value)
  );
  CREATE INDEX links_value ON links(kind, value);

  CREATE TABLE checkpoints (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    thread_id TEXT NOT NULL REFERENCES threads(id),
    note TEXT NOT NULL,
    frontier_json TEXT NOT NULL,
    created_at TEXT NOT NULL
  );

  CREATE TABLE sessions (
    session_id TEXT PRIMARY KEY,
    thread_id TEXT NOT NULL REFERENCES threads(id),
    cwd TEXT,
    bound_at TEXT NOT NULL
  );
  CREATE INDEX sessions_cwd ON sessions(cwd, bound_at);

  CREATE VIRTUAL TABLE nodes_fts USING fts5(title, summary, body, content='nodes', content_rowid='rowid');
  CREATE TRIGGER nodes_ai AFTER INSERT ON nodes BEGIN
    INSERT INTO nodes_fts(rowid, title, summary, body) VALUES (new.rowid, new.title, new.summary, new.body);
  END;
  CREATE TRIGGER nodes_ad AFTER DELETE ON nodes BEGIN
    INSERT INTO nodes_fts(nodes_fts, rowid, title, summary, body) VALUES ('delete', old.rowid, old.title, old.summary, old.body);
  END;
  CREATE TRIGGER nodes_au AFTER UPDATE ON nodes BEGIN
    INSERT INTO nodes_fts(nodes_fts, rowid, title, summary, body) VALUES ('delete', old.rowid, old.title, old.summary, old.body);
    INSERT INTO nodes_fts(rowid, title, summary, body) VALUES (new.rowid, new.title, new.summary, new.body);
  END;
  `,"ALTER TABLE nodes ADD COLUMN confidence REAL CHECK (confidence IS NULL OR (confidence >= 0 AND confidence <= 1));"];function $(d){let t=d;if(!t){let n=O();F(n,{recursive:!0}),t=A(n,"state.db")}let e=new M(t);return t!==":memory:"&&e.exec("PRAGMA journal_mode = WAL"),e.exec("PRAGMA foreign_keys = ON; PRAGMA busy_timeout = 5000;"),B(e),e}function B(d){let{user_version:t}=d.prepare("PRAGMA user_version").get();for(let e=t;e<L.length;e++){d.exec("BEGIN");try{d.exec(L[e]),d.exec(`PRAGMA user_version = ${e+1}`),d.exec("COMMIT")}catch(n){throw d.exec("ROLLBACK"),n}}}import{execFileSync as X}from"node:child_process";import{realpathSync as K}from"node:fs";function S(d,t){try{return X("git",["-C",d,...t],{encoding:"utf8",stdio:["ignore","pipe","ignore"],timeout:3e3}).trim()||null}catch{return null}}function _(d){let t=d;try{t=K(d)}catch{}let e=S(t,["rev-parse","--path-format=absolute","--git-common-dir"]);if(!e)return[{kind:"dir",value:t}];let n=[{kind:"repo",value:e}],i=S(t,["remote","get-url","origin"]);return i&&n.push({kind:"repo",value:i}),n}var h=class extends Error{},T=["done","abandoned"],w=class{bound=null;db;cwd;now;constructor(t,e=process.cwd(),n=()=>new Date){this.db=t,this.cwd=e,this.now=n}ts(){return this.now().toISOString()}nextId(t){let e=this.db.prepare("UPDATE counters SET next = next + 1 WHERE name = ? RETURNING next - 1 AS id").get(t);return`${t}${e.id}`}tx(t){this.db.exec("BEGIN IMMEDIATE");try{let e=t();return this.db.exec("COMMIT"),e}catch(e){throw this.db.exec("ROLLBACK"),e}}touch(t){this.db.prepare("UPDATE threads SET touched_at = ? WHERE id = ?").run(this.ts(),t)}getThread(t){let e=this.db.prepare("SELECT * FROM threads WHERE id = ?").get(t);if(!e)throw new h(`No thread ${t}`);return e}getNode(t){let e=this.db.prepare("SELECT * FROM nodes WHERE id = ?").get(t);if(!e)throw new h(`No node ${t}`);return e}blockers(t){return this.db.prepare(`SELECT n.* FROM edges e JOIN nodes n ON n.id = e.from_id
         WHERE e.to_id = ? AND e.type = 'blocks' AND n.status NOT IN ('done','abandoned')`).all(t)}children(t){return this.db.prepare("SELECT * FROM nodes WHERE parent_id = ? ORDER BY priority DESC, rowid").all(t)}depth(t){let e=0,n=t.parent_id;for(;n;)e++,n=this.db.prepare("SELECT parent_id FROM nodes WHERE id = ?").get(n).parent_id;return e}createThread(t,e,n=!0,i){return this.tx(()=>{let o=this.nextId("t"),c=this.ts();if(this.db.prepare("INSERT INTO threads (id, title, goal, created_at, touched_at) VALUES (?, ?, ?, ?, ?)").run(o,t,e,c,c),n)for(let l of _(this.cwd))this.addLink(o,l);return this.bindInner(o,i),this.getThread(o)})}addLink(t,e){this.db.prepare("INSERT OR IGNORE INTO links (thread_id, kind, value) VALUES (?, ?, ?)").run(t,e.kind,e.value)}listThreads(t="active"){let e=t==="all"?"SELECT * FROM threads ORDER BY touched_at DESC":"SELECT * FROM threads WHERE status = ? ORDER BY touched_at DESC",n=this.db.prepare(e);return t==="all"?n.all():n.all(t)}threadsForLocation(t){if(!t.length)return[];let e=t.map(()=>"(l.kind = ? AND l.value = ?)").join(" OR ");return this.db.prepare(`SELECT DISTINCT t.* FROM threads t JOIN links l ON l.thread_id = t.id
         WHERE t.status = 'active' AND (${e}) ORDER BY t.touched_at DESC`).all(...t.flatMap(n=>[n.kind,n.value]))}bindInner(t,e){this.bound=t,this.db.prepare(`INSERT INTO sessions (session_id, thread_id, cwd, bound_at) VALUES (?, ?, ?, ?)
         ON CONFLICT(session_id) DO UPDATE SET thread_id = excluded.thread_id, cwd = excluded.cwd, bound_at = excluded.bound_at`).run(e??`cwd:${this.cwd}`,t,this.cwd,this.ts()),this.touch(t)}bind(t,e){let n=this.getThread(t);return this.bindInner(n.id,e),n}current(){if(this.bound)return this.getThread(this.bound);let t=this.db.prepare(`SELECT s.thread_id FROM sessions s JOIN threads t ON t.id = s.thread_id
         WHERE s.cwd = ? AND t.status = 'active' ORDER BY s.bound_at DESC LIMIT 1`).get(this.cwd);if(t)return this.bound=t.thread_id,this.getThread(t.thread_id);let e=this.threadsForLocation(_(this.cwd));if(e.length===1)return this.bound=e[0].id,e[0];let n=e.length?`Linked threads here: ${e.map(i=>`${i.id} "${i.title}"`).join(", ")}.`:"Use `autoplan threads` or `autoplan create`.";throw new h(`No thread bound. Run \`autoplan bind <thread_id>\`. ${n}`)}add(t){let e=this.current();return this.tx(()=>{let n=this.ts(),i=[],o=r=>{let a=/^#(\d+)$/.exec(r);if(a){let p=i[Number(a[1])];if(!p)throw new h(`Reference ${r} must point to an earlier item in this call`);return p}let s=this.getNode(r);if(s.thread_id!==e.id)throw new h(`${r} belongs to thread ${s.thread_id}`);return s.id},c=this.db.prepare(`INSERT INTO nodes (id, thread_id, parent_id, kind, title, body, priority, created_at, updated_at)
         VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)`),l=this.db.prepare("INSERT OR IGNORE INTO edges (from_id, to_id, type) VALUES (?, ?, 'blocks')"),u=[];for(let r of t){let a=this.nextId("n");i.push(a),c.run(a,e.id,r.parent?o(r.parent):null,r.kind??"task",r.title,r.body??null,r.priority??0,n,n);for(let s of r.blocked_by??[])l.run(o(s),a);for(let s of r.blocks??[])u.push([a,s])}for(let[r,a]of u){let s=o(a);if(s===r)throw new h("A node cannot block itself");l.run(r,s)}return this.touch(e.id),i.map(r=>this.getNode(r))})}start(t){let e=this.getNode(t);if(T.includes(e.status))throw new h(`${t} is ${e.status}; reopen it with update first`);let n=this.blockers(t);if(n.length)throw new h(`${t} is blocked by ${n.map(i=>`${i.id} "${i.title}"`).join(", ")}`);if(e.status==="blocked")throw new h(`${t} is marked blocked; update its status to open first`);return this.tx(()=>{let i=this.ts(),o=this.db.prepare("SELECT id FROM nodes WHERE thread_id = ? AND status = 'active' AND id != ?").all(e.thread_id,t).map(c=>c.id);return this.db.prepare("UPDATE nodes SET status = 'open', updated_at = ? WHERE thread_id = ? AND status = 'active' AND id != ?").run(i,e.thread_id,t),this.db.prepare("UPDATE nodes SET status = 'active', updated_at = ? WHERE id = ?").run(i,t),this.touch(e.thread_id),{node:this.getNode(t),demoted:o}})}done(t,e,n){if(!e?.trim())throw new h("done requires a non-empty summary");let i=this.getNode(t);if(i.status==="done")throw new h(`${t} is already done`);let o=this.children(t).filter(c=>!T.includes(c.status));if(o.length)throw new h(`${t} has unresolved children: ${o.map(c=>c.id).join(", ")}`);return this.tx(()=>this.complete(i,e.trim(),n,this.ts()))}complete(t,e,n,i){this.db.prepare("UPDATE nodes SET status = 'done', summary = ?, refs = ?, updated_at = ? WHERE id = ?").run(e,n?.length?JSON.stringify(n):null,i,t.id);let o=this.releaseDependents(t.id,i);return this.touch(t.thread_id),{node:this.getNode(t.id),unblocked:o,parentReady:this.parentReady(t.parent_id)}}releaseDependents(t,e){let n=this.db.prepare(`SELECT n.* FROM edges e JOIN nodes n ON n.id = e.to_id
         WHERE e.from_id = ? AND e.type = 'blocks' AND n.status NOT IN ('done','abandoned')`).all(t),i=[];for(let o of n)this.blockers(o.id).length||(o.status==="blocked"&&this.db.prepare("UPDATE nodes SET status = 'open', updated_at = ? WHERE id = ?").run(e,o.id),i.push(this.getNode(o.id)));return i}parentReady(t){if(!t)return null;let e=this.getNode(t);return T.includes(e.status)?null:this.children(t).every(n=>T.includes(n.status))?e:null}recordFinding(t,e,n,i,o=!1){if(!e?.trim())throw new h("A finding needs text");if(n!==void 0&&!(n>=0&&n<=1))throw new h("confidence must be between 0 and 1");let c=this.getNode(t);if(c.kind==="finding")throw new h(`${t} is a finding; attach to the question it informs`);if(o){if(c.kind!=="question")throw new h(`${t} is a ${c.kind}; only questions can be answered`);if(T.includes(c.status))throw new h(`${t} is already ${c.status}`);let u=this.children(t).filter(r=>!T.includes(r.status));if(u.length)throw new h(`${t} has unresolved children: ${u.map(r=>r.id).join(", ")}`)}let l=e.trim();return this.tx(()=>{let u=this.nextId("n"),r=this.ts();this.db.prepare(`INSERT INTO nodes (id, thread_id, parent_id, kind, title, status, summary, refs, confidence, created_at, updated_at)
           VALUES (?, ?, ?, 'finding', ?, 'done', ?, ?, ?, ?, ?)`).run(u,c.thread_id,c.id,y(l.split(`
`)[0],100),l,i?.length?JSON.stringify(i):null,n??null,r,r);let a=null;return o&&(this.db.prepare("INSERT INTO edges (from_id, to_id, type) VALUES (?, ?, 'answers')").run(u,c.id),a=this.complete(c,`Answered by ${u}: ${l}`,i,r)),this.touch(c.thread_id),{node:this.getNode(u),closed:a}})}evidence(t){let e=this.db.prepare("SELECT COUNT(*) AS count, MAX(confidence) AS best FROM nodes WHERE parent_id = ? AND kind = 'finding'").get(t);return{count:Number(e.count),best:e.best}}update(t,e){let n=this.getNode(t);if(e.status==="done")throw new h("Use done(id, summary) to complete a node");if(e.status==="abandoned"&&!(e.summary??n.summary)?.trim())throw new h("Abandoning requires a summary explaining why");if(e.status==="active")throw new h("Use start(id) to activate a node");let i=[],o=[];for(let c of["title","body","status","priority","summary","kind"])e[c]!==void 0&&(i.push(`${c} = ?`),o.push(e[c]));if(!i.length)throw new h("No fields to update");return this.tx(()=>{let c=this.ts();this.db.prepare(`UPDATE nodes SET ${i.join(", ")}, updated_at = ? WHERE id = ?`).run(...o,c,t);let l=e.status==="abandoned"?this.releaseDependents(t,c):[];return this.touch(n.thread_id),{node:this.getNode(t),unblocked:l}})}nextOptions(t=3,e){let n=e??this.current().id,i=this.db.prepare("SELECT * FROM nodes WHERE thread_id = ? AND status = 'open' AND kind IN ('task','question')").all(n),o=this.now().getTime(),c=[];for(let l of i){if(this.blockers(l.id).length)continue;let u=this.children(l.id).filter(m=>!T.includes(m.status)).length,r=this.depth(l),a=Math.min(7,(o-Date.parse(l.updated_at))/864e5),s=u===0?5:0,p=0,f=null;if(l.kind==="question"){let m=this.evidence(l.id);m.count?(m.best==null||m.best<.7)&&(p=3*(1-(m.best??.5)),f=`low confidence ${m.best??"unrated"}`):[p,f]=[3,"unexplored"]}let g=l.priority*10+s+r*1.5+a*.5+p,x=[l.priority?`p${l.priority}`:null,u?`${u} open children`:"leaf",f,r?`depth ${r}`:null,a>=1?`idle ${Math.floor(a)}d`:null].filter(Boolean).join(", ");c.push({node:l,score:Math.round(g*10)/10,why:x})}return c.sort((l,u)=>u.score-l.score||l.node.created_at.localeCompare(u.node.created_at)||l.node.id.localeCompare(u.node.id,void 0,{numeric:!0})),c.slice(0,t)}search(t,e={}){let n=j(t),i=e.all?null:this.current().id;return this.db.prepare(`SELECT n.*, t.title AS thread_title,
                snippet(nodes_fts, -1, '[', ']', '\u2026', 12) AS snippet
         FROM nodes_fts JOIN nodes n ON n.rowid = nodes_fts.rowid JOIN threads t ON t.id = n.thread_id
         WHERE nodes_fts MATCH ? AND (? IS NULL OR n.thread_id = ?) AND (? IS NULL OR n.kind = ?)
         ORDER BY bm25(nodes_fts, 10, 5, 1) LIMIT ?`).all(n,i,i,e.kind??null,e.kind??null,e.limit??10).map(o=>{let{thread_title:c,snippet:l,...u}=o;return{node:u,snippet:l,thread_title:c}})}related(t,e=3){let n=this.getThread(t),i=[...new Set(`${n.title} ${n.goal??""}`.toLowerCase().match(/[\p{L}\p{N}_]+/gu)??[])].filter(o=>o.length>=3&&!W.has(o));return i.length?this.db.prepare(`SELECT n.*, t.title AS thread_title,
                snippet(nodes_fts, -1, '[', ']', '\u2026', 8) AS snippet
         FROM nodes_fts JOIN nodes n ON n.rowid = nodes_fts.rowid JOIN threads t ON t.id = n.thread_id
         WHERE nodes_fts MATCH ? AND n.thread_id != ? AND n.kind IN ('finding','decision') AND n.status = 'done'
         ORDER BY bm25(nodes_fts, 10, 5, 1) LIMIT ?`).all(i.map(o=>`"${o}"*`).join(" OR "),n.id,e).map(o=>{let{thread_title:c,snippet:l,...u}=o;return{node:u,snippet:l,thread_title:c}}):[]}checkpoint(t){if(!t?.trim())throw new h("checkpoint requires a note");let e=this.current(),i={active:this.db.prepare("SELECT id FROM nodes WHERE thread_id = ? AND status = 'active'").all(e.id).map(c=>c.id),next:this.nextOptions(5,e.id).map(c=>c.node.id)},o=this.db.prepare("INSERT INTO checkpoints (thread_id, note, frontier_json, created_at) VALUES (?, ?, ?, ?)").run(e.id,t.trim(),JSON.stringify(i),this.ts());return this.touch(e.id),{id:Number(o.lastInsertRowid)}}statusText(t){let e=t?this.getThread(t):this.current(),n=Object.fromEntries(this.db.prepare("SELECT status, COUNT(*) AS c FROM nodes WHERE thread_id = ? GROUP BY status").all(e.id).map(s=>[s.status,s.c])),i=this.db.prepare("SELECT * FROM nodes WHERE thread_id = ? AND status = 'active'").all(e.id),o=this.db.prepare("SELECT * FROM nodes WHERE thread_id = ? AND status IN ('open','blocked') ORDER BY priority DESC").all(e.id).filter(s=>s.status==="blocked"||this.blockers(s.id).length),c=this.db.prepare("SELECT note, created_at FROM checkpoints WHERE thread_id = ? ORDER BY id DESC LIMIT 1").get(e.id),l=[`[autoplan] ${e.id} "${e.title}" (${e.status})`];e.goal&&l.push(`Goal: ${y(e.goal,200)}`);let u=["open","active","blocked","done","abandoned"].filter(s=>n[s]).map(s=>`${n[s]} ${s}`).join(", ");l.push(`Nodes: ${u||"none"}`),i.length&&l.push(`Active: ${i.map(b).join("; ")}`);let r=this.nextOptions(3,e.id);if(r.length){l.push("Next:");for(let s of r)l.push(`  ${b(s.node)}`)}if(o.length){l.push("Blocked:");for(let s of o.slice(0,5)){let p=this.blockers(s.id).map(f=>f.id);l.push(`  ${b(s)}${p.length?` \u2190 ${p.join(", ")}`:""}`)}o.length>5&&l.push(`  \u2026${o.length-5} more`)}let a=this.related(e.id);if(a.length){l.push("Related (other threads):");for(let s of a)l.push(`  ${b(s.node)} [${s.node.thread_id}]: ${y(s.node.summary??s.snippet,120)}`)}return c&&l.push(`Last checkpoint (${c.created_at.slice(0,16)}): ${y(c.note,400)}`),l.join(`
`)}getText(t,e=0){let n=this.getNode(t),i=[`${n.id} [${n.kind}/${n.status}${n.priority?` p${n.priority}`:""}${n.confidence!=null?` conf ${n.confidence}`:""}] ${n.title}`,`thread ${n.thread_id}${n.parent_id?`, parent ${n.parent_id}`:""}, updated ${n.updated_at.slice(0,16)}`];n.body&&i.push(`Body: ${n.body}`),n.summary&&i.push(`Summary: ${n.summary}`),n.refs&&i.push(`Refs: ${JSON.parse(n.refs).join(", ")}`);let o=this.blockers(t);o.length&&i.push(`Blocked by: ${o.map(b).join("; ")}`);let c=this.db.prepare("SELECT to_id FROM edges WHERE from_id = ? AND type = 'blocks'").all(t);c.length&&i.push(`Blocks: ${c.map(u=>u.to_id).join(", ")}`);let l=(u,r,a)=>{for(let s of this.children(u))i.push(`${a}${b(s)}${s.confidence!=null?` (conf ${s.confidence})`:""}${s.summary?` \u2014 ${y(s.summary,120)}`:""}`),r>1&&l(s.id,r-1,a+"  ")};if(e>0)i.push("Children:"),l(t,e,"  ");else{let u=this.children(t).length;u&&i.push(`${u} children (use depth>0 to list)`)}return i.join(`
`)}resume(t){let e=_(this.cwd),n=this.threadsForLocation(e);if(t){let i=this.db.prepare(`SELECT s.thread_id FROM sessions s JOIN threads t ON t.id = s.thread_id
           WHERE s.session_id = ? AND t.status = 'active'`).get(t);if(i)return this.bindInner(i.thread_id,t),this.statusText(i.thread_id)}return n.length===1?(this.bindInner(n[0].id,t),this.statusText(n[0].id)):n.length>1?["[autoplan] Multiple active threads are linked to this location. Ask the user which one, then run `autoplan bind <thread_id>`:",...n.map(i=>`  ${i.id} "${i.title}" (touched ${i.touched_at.slice(0,10)})`)].join(`
`):""}};function j(d){let t=[];for(let e of d.matchAll(/"([^"]*)"|(\S+)/g))if(e[1]!==void 0){let n=e[1].match(/[\p{L}\p{N}_]+/gu);n&&t.push(`"${n.join(" ")}"`)}else for(let n of e[2].match(/[\p{L}\p{N}_]+/gu)??[])t.push(`"${n}"*`);if(!t.length)throw new h("search needs at least one word");return t.join(" ")}var W=new Set("and are but can for has how its not the use was why about after also been before being both could does from have into just like more most much only other over same should some such than that their them then there these they this those through very what when where which while will with would your".split(" "));function y(d,t){return d.length>t?d.slice(0,t-1)+"\u2026":d}function b(d){return`${d.id} ${d.kind==="task"?"":`(${d.kind}) `}${d.title}`}var D=`usage: autoplan <command> [args] [--cwd DIR] [--thread ID]
  status                                   compact view of the bound thread
  threads [--all]                          list threads (* = bound here)
  create TITLE --goal G [--no-link]        create thread, link this dir, bind
  bind THREAD_ID                           bind this dir to a thread
  add TITLE [--kind K] [--parent ID] [--body B] [--priority N] [--blocks ID,..] [--blocked-by ID,..]
  add -                                    add items from a JSON array on stdin
                                           ({title, kind?, parent?, body?, priority?, blocks?, blocked_by?};
                                            "#i" refers to the i-th item of the same array)
  start ID                                 mark active (refuses if blocked)
  done ID --summary S [--ref R]...         complete; summary required
  finding ID TEXT [--confidence 0..1] [--source S]... [--answers]
                                           record a finding under question/node ID
                                           (--answers: also close question ID with it)
  update ID [--title T] [--body B] [--status open|blocked|abandoned] [--priority N] [--summary S] [--kind K]
  next [-n N]                              ranked options to work on next
  get ID [--depth D]                       full node detail (+ subtree)
  search QUERY [--all] [--kind K] [-n N]   full-text search this thread (--all: every thread);
                                           words match as prefixes, "quoted phrases" exactly
  checkpoint NOTE                          save handoff note
  resume [--session ID] [--hook]           SessionStart: bind + print status
                                           (--hook: read {cwd, session_id} JSON from stdin)`,C=["task","question","finding","decision"];function U(){return J(0,"utf8")}function Q(){try{return JSON.parse(U())}catch{return{}}}function z(){try{let d=I(O(),"bin");Y(d,{recursive:!0});let t=I(d,"autoplan");q(t,`#!/bin/sh
exec node "${V(import.meta.url)}" "$@"
`),P(t,493)}catch{}}var N=d=>`${d.id} [${d.kind}/${d.status}] ${d.title}`;function E(d,t){if(!d)throw new h(`Missing ${t}. Run 'autoplan help' for usage.`);return d}function k(d){if(d!==void 0&&!C.includes(d))throw new h(`--kind must be one of ${C.join(", ")}`);return d}function R(d,t){if(d===void 0)return;let e=Number(d);if(!Number.isInteger(e))throw new h(`${t} must be an integer`);return e}var v=d=>d?d.split(",").map(t=>t.trim()).filter(Boolean):void 0;function Z(d){let t;try{t=JSON.parse(d)}catch(n){throw new h(`stdin is not valid JSON: ${n.message}`)}Array.isArray(t)||(t=[t]);let e=t;if(!e.length)throw new h("No items to add");for(let[n,i]of e.entries()){if(!i||typeof i.title!="string"||!i.title.trim())throw new h(`Item #${n} needs a title`);k(i.kind)}return e}function tt(d=process.argv.slice(2)){let{values:t,positionals:e}=G({args:d,allowPositionals:!0,options:{cwd:{type:"string"},session:{type:"string"},thread:{type:"string"},hook:{type:"boolean"},answers:{type:"boolean"},all:{type:"boolean"},goal:{type:"string"},"no-link":{type:"boolean"},kind:{type:"string"},parent:{type:"string"},body:{type:"string"},priority:{type:"string"},blocks:{type:"string"},"blocked-by":{type:"string"},summary:{type:"string"},ref:{type:"string",multiple:!0},confidence:{type:"string"},source:{type:"string",multiple:!0},title:{type:"string"},status:{type:"string"},n:{type:"string",short:"n"},depth:{type:"string"}}}),[n,...i]=e,o=i.join(" ")||void 0,c=t.cwd,l=t.session;if(t.hook){let a=Q();c??=a.cwd,l??=a.session_id}let u=new w($(),c??process.cwd());t.thread&&(u.bound=u.getThread(t.thread).id);let r=a=>console.log(a);switch(n){case"resume":{z();let a=u.resume(l);if(!a)return 0;if(t.hook){let s=a.split(`
`),p=s.find(g=>g.startsWith("Nodes:")),f=[s[0].replace(/^\[autoplan\] /,"autoplan: resumed "),p?.replace(/^Nodes: /,"")].filter(Boolean).join(" \u2014 ");r(JSON.stringify({systemMessage:f,hookSpecificOutput:{hookEventName:"SessionStart",additionalContext:a}}))}else r(a);return 0}case"status":return r(u.statusText()),0;case"threads":{let a=u.listThreads(t.all?"all":"active"),s=null;try{s=u.current().id}catch{}a.length||r("No threads.");for(let p of a)r(`${p.id}${p.id===s?"*":""} [${p.status}] ${p.title} (touched ${p.touched_at.slice(0,10)})`);return 0}case"create":{let a=u.createThread(E(o,"TITLE"),E(t.goal,"--goal"),!t["no-link"]);return r(`Created and bound ${a.id} "${a.title}".`),0}case"bind":return u.bind(E(o,"THREAD_ID")),r(u.statusText()),0;case"add":{let a=o==="-"?Z(U()):[{title:E(o,"TITLE (or '-' for JSON on stdin)"),kind:k(t.kind),parent:t.parent,body:t.body,priority:R(t.priority,"--priority"),blocks:v(t.blocks),blocked_by:v(t["blocked-by"])}];for(let s of u.add(a))r(N(s));return 0}case"start":{let{node:a,demoted:s}=u.start(E(o,"ID"));return r(`Started ${N(a)}`),s.length&&r(`Returned to open: ${s.join(", ")}`),0}case"done":{let{node:a,unblocked:s,parentReady:p}=u.done(E(o,"ID"),E(t.summary,"--summary"),t.ref);return r(`Done ${N(a)}`),s.length&&r(`Unblocked: ${s.map(N).join("; ")}`),p&&r(`All children of ${p.id} "${p.title}" are resolved \u2014 consider: autoplan done ${p.id}`),0}case"finding":{let[a,...s]=i,p;if(t.confidence!==void 0&&(p=Number(t.confidence),Number.isNaN(p)))throw new h("--confidence must be a number between 0 and 1");let{node:f,closed:g}=u.recordFinding(E(a,"ID"),E(s.join(" ")||void 0,"TEXT"),p,t.source,t.answers);return r(`Recorded ${N(f)}${f.confidence!=null?` (conf ${f.confidence})`:""} under ${f.parent_id}`),g&&(r(`Answered ${N(g.node)}`),g.unblocked.length&&r(`Unblocked: ${g.unblocked.map(N).join("; ")}`),g.parentReady&&r(`All children of ${g.parentReady.id} "${g.parentReady.title}" are resolved \u2014 consider: autoplan done ${g.parentReady.id}`)),0}case"update":{if(t.status!==void 0&&!["open","blocked","abandoned"].includes(t.status))throw new h("--status must be open, blocked, or abandoned (use start/done otherwise)");let{node:a,unblocked:s}=u.update(E(o,"ID"),{title:t.title,body:t.body,status:t.status,priority:R(t.priority,"--priority"),summary:t.summary,kind:k(t.kind)});return r(`Updated ${N(a)}`),s.length&&r(`Unblocked: ${s.map(N).join("; ")}`),0}case"next":{let a=u.nextOptions(R(t.n,"-n")??3);a.length||r("Nothing open and unblocked.");for(let s of a)r(`${N(s.node)}  (score ${s.score}: ${s.why})`);return 0}case"get":return r(u.getText(E(o,"ID"),R(t.depth,"--depth")??0)),0;case"search":{let a=u.search(E(o,"QUERY"),{all:t.all,kind:k(t.kind),limit:R(t.n,"-n")??10});a.length||r("No matches.");for(let s of a)r(`${N(s.node)}${t.all?` (${s.node.thread_id} "${s.thread_title}")`:""}
    ${s.snippet.replace(/\s+/g," ")}`);return 0}case"checkpoint":{let{id:a}=u.checkpoint(E(o,"NOTE"));return r(`Checkpoint #${a} saved.`),0}case"help":case void 0:return r(D),n?0:2;default:return console.error(`Unknown command '${n}'.
${D}`),2}}try{process.exitCode=tt()}catch(d){if(!(d instanceof h)&&!(d instanceof TypeError&&"code"in d))throw d;console.error(d.message),process.exitCode=1}
